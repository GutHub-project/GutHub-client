// @repo/shared의 dietApi를 재export
export { dietApi } from '@repo/shared';

// 타입도 재export
export type { Food, DietLog, CreateDietRequest, UpdateDietRequest, MealType } from '@repo/shared';
